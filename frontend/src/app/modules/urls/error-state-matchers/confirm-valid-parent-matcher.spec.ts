import { ConfirmValidParentMatcher } from './confirm-valid-parent-matcher';

describe('ConfirmValidParentMatcher', () => {
    it('should create an instance', () => {
        expect(new ConfirmValidParentMatcher()).toBeTruthy();
    });
});
