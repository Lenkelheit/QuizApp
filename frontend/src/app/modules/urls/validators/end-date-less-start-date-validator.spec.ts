import { EndDateLessStartDateValidator } from './end-date-less-start-date-validator';

describe('EndDateLessStartDateValidator', () => {
  it('should create an instance', () => {
    expect(new EndDateLessStartDateValidator()).toBeTruthy();
  });
});
