import { FormatTimeLimitValidator } from './format-time-limit-validator';

describe('FormatTimeLimitValidator', () => {
    it('should create an instance', () => {
        expect(new FormatTimeLimitValidator()).toBeTruthy();
    });
});
